{
  "name": "todoer-cli",
  "version": "0.1.0",
  "description": "Strict CLI for managing TODO.md and TODO_COMPLETED.md task files.",
  "main": "src/cli.js",
  "bin": {
    "todoer-cli": "src/cli.js"
  },
  "scripts": {
    "test": "vitest run",
    "lint:js": "eslint .",
    "lint:tasks": "node src/lint.js",
    "lint": "npm run lint:js && npm run lint:tasks"
  },
  "keywords": [
    "todo",
    "cli",
    "ollama"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "neo-blessed": "^0.2.0"
  },
  "devDependencies": {
    "eslint": "^10.2.0",
    "node-pty": "^1.1.0",
    "strip-ansi": "^7.2.0",
    "vitest": "^4.1.2"
  }
}
